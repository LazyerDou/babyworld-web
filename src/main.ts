// PocketSeed marketing site — static multi-page Vite build.
// The HTML pages are self-contained; this entry exists only so Vite
// has a JS chunk to attach styles to and so each page link references
// the same processed CSS.
import './style.css';
